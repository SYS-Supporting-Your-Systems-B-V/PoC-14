# SQLite default (for local development)
PDQM_DB_URL=sqlite:///./pdqm.db

# Base URL of the LDAP proxy used by ldap_zoek.html in the browser.
# For local docker-compose on this repo:
PDQM_LDAP_BASE=http://localhost:8002

# Base URL of the mCSD proxy used by mcsd_zoek.html in the browser.
# For local docker-compose on this repo:
PDQM_MCSD_BASE=http://localhost:8000
